# OpenClaw SkillHub MCP Bundle

This bundle declares the local SkillHub stdio MCP server for OpenClaw.

```bash
openclaw plugins install ./openclaw-skillhub-mcp-bundle
openclaw plugins inspect skillhub-mcp --runtime
```
